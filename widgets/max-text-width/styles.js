/******************************************************************************/

export default function styles() {
  const maxTextWidth = {
    display: 'inline-block',
  };

  const hidden = {
    height: 0,
    overflow: 'hidden',
  };

  const visible = {};

  return {
    maxTextWidth,
    hidden,
    visible,
  };
}

/******************************************************************************/
