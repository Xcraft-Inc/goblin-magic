export default function styles() {
  const inlineIcon = {
    display: 'inline-block',
  };

  const icon = {
    verticalAlign: 'middle',
    position: 'relative',
    top: '-1.6px',
    boxSizing: 'content-box',
    margin: '-15% 0',
    paddingTop: '4%',
  };

  return {
    inlineIcon,
    icon,
  };
}
