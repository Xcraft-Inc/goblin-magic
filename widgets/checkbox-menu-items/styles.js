export default function styles() {
  const items = {
    padding: '3px 3px 2px 3px',
  };

  const item = {
    display: 'flex',
    flexDirection: 'row',
  };

  const hr = {
    width: '100%',
    margin: 0,
  };

  return {
    items,
    item,
    hr,
  };
}
