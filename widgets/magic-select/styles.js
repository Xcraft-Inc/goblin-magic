export default function styles() {
  const selectButton = {
    flexGrow: 0,
  };

  const chevronDown = {
    position: 'relative',
    top: '1px',
    fontSize: '13px',
  };

  return {
    selectButton,
    chevronDown,
  };
}
